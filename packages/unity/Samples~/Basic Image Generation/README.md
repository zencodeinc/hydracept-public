# Basic Image Generation

Sample workflow for the Hydracept Unity package:

1. Configure workspace with `python -m hydracept quickstart`
2. Open **Window → Hydracept**
3. Use the **Generate** tab to create a transparent sprite
4. Import into `Assets/Generated/Hydracept/`

Samples are documentation-only in v0.1.0.
