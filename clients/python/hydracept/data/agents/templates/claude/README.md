# Hydracept for Claude Code

Install into a workspace with:

```bash
python -m hydracept agents install claude
```

Skills are namespaced as `/hydracept:setup`, `/hydracept:smoke`, and `/hydracept:sheet`.
