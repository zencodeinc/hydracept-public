{
  "mcpServers": {
    "hydracept": {
      "command": "python",
      "args": ["-m", "hydracept", "mcp", "serve", "--workspace", "${workspaceFolder}"],
      "env": {
        "HYDRACEPT_WORKSPACE": "${workspaceFolder}"
      }
    }
  }
}
