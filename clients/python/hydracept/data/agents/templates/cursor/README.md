# Hydracept for Cursor

Install into a workspace with:

```bash
python -m hydracept agents install cursor
```

SessionStart hooks call `python -m hydracept agent-status --json` locally. MCP runs via `python -m hydracept mcp serve`.
